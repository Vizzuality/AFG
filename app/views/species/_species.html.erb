<div class="specie"> 
  <a href="<%= species_path(species) %>"><img src="" alt="<%= species.full_name %>" /></a>
  <h4><%= link_to(truncate(species.full_name, :length => 13, :omission =>"…"), species_path(species)) %></h4>
  <p class="heart <%= species.guides_count==0 ? 'zero' : '' %>"><span><img src="<%= species.guides_count==0 ? 'images/home/gray_heart.png' : 'images/home/pink_heart.png' %>" alt="heart"/><%= species.guides_count %></span></p> 
  <% if species.common_name? %>
  <p class="taxa"><%= species.common_name %></p> 
  <% end -%>
</div> 
